const assert = require('assert');

const temperatureCtrl = require('../controllers/temperature.controller');

describe('Should return the maximun value of the list', () => {
    it('should return 5', () => {
        /*
        TODO
        1. Separate logic from Controller to indepent class
        2. Create method for pass list of values or records
        3. Crate method for get each statistcs
        */

        assert.strictEqual(1 + 1, 2);
    });

});